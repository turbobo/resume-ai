## ResumeAI 部署到 Vercel 完整指南

### 前置准备

你需要：
1. GitHub 账号（已有）
2. Vercel 账号（免费注册：https://vercel.com/signup，用 GitHub 登录即可）
3. Supabase 账号（免费注册：https://supabase.com）
4. （可选）DashScope API Key（免费：https://dashscope.console.aliyun.com）

---

### 第一步：注册 Supabase 并创建项目

1. 打开 https://supabase.com → Sign Up → 用 GitHub 登录
2. 点击 "New Project"
   - Name: `resume-ai`
   - Password: 设一个记住
   - Region: 选 Northeast Asia (Tokyo) 离中国最近
3. 等待项目初始化（约 2 分钟）
4. 左侧菜单 → Authentication → Providers → 确认 Email 已开启
5. 左侧菜单 → Project Settings → API → 复制两个值：
   - **Project URL** → 形如 `https://xxxx.supabase.co`
   - **anon public key** → 一长串字符串

---

### 第二步：推送代码到 GitHub

1. 在 GitHub 创建一个新仓库：
   - 打开 https://github.com/new
   - Repository name: `resume-ai`
   - 选 Public 或 Private
   - **不要**勾选 "Add README" 等选项
   - 点击 Create repository

2. 在终端执行（替换你的用户名）：
```bash
cd /Users/qinghang/Documents/QoderWorkSpace/MyApp/resume-ai
git remote add origin https://github.com/你的用户名/resume-ai.git
git branch -M main
git push -u origin main
```

> 如果 push 超时，可以尝试配置代理：
> ```bash
> git config --global http.proxy http://127.0.0.1:你的代理端口
> ```

---

### 第三步：在 Vercel 部署

1. 打开 https://vercel.com → 用 GitHub 账号登录
2. 点击 "Add New Project"
3. 找到 `resume-ai` 仓库 → 点击 "Import"
4. 在 Environment Variables 部分，添加以下变量：

| Key | Value |
|-----|-------|
| NEXT_PUBLIC_SUPABASE_URL | 你的 Supabase Project URL |
| NEXT_PUBLIC_SUPABASE_ANON_KEY | 你的 Supabase anon key |
| DASHSCOPE_API_KEY | 你的通义千问 API Key（可选） |

5. 点击 "Deploy"
6. 等待 30-60 秒，部署完成！
7. 你会得到一个 URL，如 `https://resume-ai-xxxx.vercel.app`

---

### 第四步：验证

1. 打开部署后的 URL
2. 测试以下功能：
   - 首页正常加载
   - 点击"开始使用"进入编辑器
   - 手动填写简历内容
   - 点击 AI 分析（未配置 DashScope 会使用模拟数据）
   - 选择模板并预览

---

### 后续更新

每次修改代码后，只需：
```bash
git add .
git commit -m "update: xxx"
git push
```
Vercel 会自动重新部署，无需手动操作。

---

### 自定义域名（可选）

Vercel 免费支持自定义域名：
1. 在域名注册商购买域名（如 Cloudflare $10/年）
2. Vercel 项目 → Settings → Domains → 添加域名
3. 按提示添加 DNS 记录
4. 自动 HTTPS

---

### 免费额度

| 项目 | Vercel 免费额度 | 超限费用 |
|------|---------------|---------|
| 部署次数 | 无限 | - |
| 带宽 | 100GB/月 | $0.15/GB |
| 构建时间 | 6000分钟/月 | $0.50/分钟 |
| Serverless 调用 | 100GB-hrs/月 | 按量 |
| 团队成员 | 仅自己 | Pro $20/月 |

对于个人项目，100GB/月带宽足够支撑约 1 万用户。
